import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ariel.aihub',
  appName: 'AI-Hub',
  webDir: 'dist/ai-hub-ui/browser'
};

export default config;
